<%= form_for(want) do |f| %>
  <% if want.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(want.errors.count, "error") %> prohibited this want from being saved:</h2>

      <ul>
      <% want.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :about %>
    <%= f.text_area :about %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
